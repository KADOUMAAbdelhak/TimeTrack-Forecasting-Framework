# Final Experiment Plan (FGCS C1)

Generated by `scripts/plan_final_experiments.py` prior to freeze.

- Unique base-model fits: **5280**
- Reconciliation evaluations: **7920**
- HPO trials: **16992**
- Expected CPU-hours: **34.6**
- Expected disk (GB): **0.55**
- Expected artifact count: **5306**

## Per-stage runtime (hours)

```json
{
  "base_fits": 15.6,
  "hpo": 15.6,
  "reconciliation_stats_reporting": 3.5
}
```

## Retry / timeout

```json
{
  "retry": {
    "max_retries_per_run": 1,
    "skip_on_persistent_timeout": true
  },
  "timeouts": {
    "dlinear_per_run_sec": 180,
    "lstm_per_run_sec": 300,
    "lightgbm_per_run_sec": 120,
    "ridge_per_run_sec": 60,
    "reconciliation_per_run_sec": 30
  }
}
```

## Notes

- Estimates assume fixed post-HPO context (not refitting all contexts).
- Lower HPO budgets before freeze if CPU-hours exceed available wall-clock.
- DLinear remains timeout-bounded; failures recorded as metadata.

## Freeze gate

Do not freeze if CPU-hours are unexpectedly unbounded or smoke fails.
